# 人物参考图指南

## 人物数量规划

在旁白定稿后、生成任何参考图前，先通读完整讲稿和全部片段规划，建立全片人物清单。

- 人物参考图的数量由影片中的关键人物数量决定，不预设 1–3 人等固定范围，也不为凑数增加角色。
- 只有会跨镜头或跨片段重复出现，或在单个重要片段中必须保持稳定身份、需要被观众识别为同一人的角色，才计入关键人物并生成人物参考图。
- 同事、乘客、路人、顾客、士兵等只承担一次性功能时，作为没有固定身份的群演处理，不生成人物参考图。
- 影片需要几个关键人物，就为几个人分别生成独立人物参考图；不得把多个不同人物合并在同一张人物参考图中。
- 先输出并保存“人物—身份职责—辨识特征—出现片段—参考图状态”对应表；人物数量和名单经用户确认后再生成。

## 人物参考图规格

人物参考图的目标是让模型稳定识别角色。视图规格由当前 Mode 决定：Cinematic Drama 必须完整读取其[视觉与声音参考资产指南](../modes/cinematic-drama/reference-development-guide.md)，让主角和跨片段配角至少拥有可直接接入视频的人物参考，并按角色重要性与制作速度决定是否补充 16:9 无文字多视图人物卡；其他 Mode 默认先使用最少而充分的参考：

- 一张图只出现一个人物，人物无遮挡、占比足够，头发、脸部、体型、服装和主要配色清楚可辨。
- 单个正面或自然三分之四视角的完整人物通常已经足够。背景可以是纯白、纯色、柔和渐变或不抢主体的简洁环境，不要求必须纯白。
- 允许平涂、少量高光、简单明暗、材质变化和符合所选 style 的背景处理；不为了统一规格抹掉已经确认的人物设计特点。
- 只有剧情确实依赖背面、转身、复杂服装背部、侧面轮廓或大幅度多角度调度时，才补充背面、侧面或多视图参考。可以使用独立图片，或在目标模型已验证不会混淆主体时使用同图多视图。
- 表情表、姿态表、年龄版本和换装版本也只在实际片段暴露出稳定性需求时增加，不把它们作为默认前置条件。
- 人物参考主要负责身份、相貌、比例、服装与材质；简洁环境可以保留，但不能遮挡主体或让模型误把场景当作必须复制的内容。

对 Animated Explainer、Storytime Animation 和其他没有专属规格的 Mode，多视图人物素材可能造成主体混淆、ID 漂移或重复人物，因此优先从单人物单视图开始。Cinematic Drama 使用用户已实测通过的无文字多视图人物卡工作流，可直接接入视频；不得把其他 Mode 的单视图默认规则用来覆盖该 Mode 的专属要求。

Animated Explainer 与 Storytime Animation 不生成独立场景参考图、画风参考图或风格参考图。Cinematic Drama 按专属资产指南生成连续性关键场景参考。所有 Mode 都仍由当前 style 的文字规则约束画风，不用抽象画风参考图取代 style。

## 输入视频模型时的图片指代

按实际连接顺序编号，只连接当前片段需要保持稳定身份的关键人物参考图；每个视频节点都根据自己的真实输入重新核对编号。

只有一个主要人物时，参考素材职责使用：

```markdown
参考图1为青年主角的人物参考图。将参考图1中短而蓬松的深棕发、灰绿色短外套的青年定义为“青年主角”。视频中的青年主角参考图1的人物形象。
```

存在多个关键人物时，按连接顺序逐个定义：

```markdown
参考图1为人物甲的人物参考图，参考图2为人物乙的人物参考图。将参考图1中具有[稳定特征]的人物定义为“人物甲”，将参考图2中具有[稳定特征]的人物定义为“人物乙”。视频中的人物甲参考图1的人物形象，人物乙参考图2的人物形象。
```

遵循以下约束：

- 用 2–3 个稳定静态特征定义每个主体，并在全部镜头中持续使用同一角色标签。
- 每次提到已定义的主要人物时都使用其固定标签，不改用“他”“她”“那个人”等可能混淆的指代。
- 人物参考图编号只来自当前节点真实连接顺序；不得引用未连接素材，也不得沿用其他片段的旧编号。
- 没有固定形象的群演不绑定人物参考图。Prompt 直接说明群演与关键人物使用相同的视觉媒介、造型语言和渲染体系，同时不得复制关键人物的具体脸、发型和服装。
- 每个片段根据具体内容写清场景地点、时代、空间结构、天气、道具、材质、色彩、光线和摄影方式；不同片段可以更换全部场景内容，只需保持所选 style 的稳定视觉语言。
